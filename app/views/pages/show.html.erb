<h1><%= @page.title %></h1>
<p>
  <%= @page.body.html_safe %>
</p>

<ul class="pagination">
  <% @pages.each do |page| %>
  <li><%= link_to_unless page.id == @page.id, page.id, page %></li>
  <% end %>
</ul>